-- Generated by source/tools/generate_package_bootstraps.py.
-- Edit source/package_manifest.json, then run the generator.

local function registrations(catalog, expected_id, module_name)
  assert(type(catalog) == "table", "Canonical catalog must return a table: " .. module_name)
  for key in next, catalog do
    assert(key == "id" or key == "registrations", "Canonical catalog has extra key: " .. module_name .. ":" .. tostring(key))
  end
  assert(catalog.id == expected_id, "Canonical catalog id mismatch: " .. module_name)
  assert(type(catalog.registrations) == "table", "Canonical catalog registrations missing: " .. module_name)
  return catalog.registrations
end


local entries = {}

local catalog_1 = require "contracts.families.ef00.sensors.z2m_absorption"
for _, entry in ipairs(registrations(catalog_1, "ef00.sensors.z2m_absorption", "contracts.families.ef00.sensors.z2m_absorption")) do
  entries[#entries + 1] = entry
end

local catalog_2 = require "contracts.families.ef00.sensors.wave16_excellux"
for _, entry in ipairs(registrations(catalog_2, "ef00.sensors.wave16_excellux", "contracts.families.ef00.sensors.wave16_excellux")) do
  entries[#entries + 1] = entry
end

local catalog_3 = require "contracts.families.ef00.sensors.wave16_lincukoo_air"
for _, entry in ipairs(registrations(catalog_3, "ef00.sensors.wave16_lincukoo_air", "contracts.families.ef00.sensors.wave16_lincukoo_air")) do
  entries[#entries + 1] = entry
end

return entries
